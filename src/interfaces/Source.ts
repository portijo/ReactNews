/**
 * Inner object field for Article interface
 *
 * @param id slugged source name
 * @param name name of the source i.e CNN, BuzzFeed, etc.
 */
export interface Source {
  id?: null | string;
  name: string;
}
