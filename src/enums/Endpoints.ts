/**
 * Endpoints enum
 *
 * @param EVERYTHING
 * @param TOP_HEADLINES
 * @param SOURCES
 */
export enum Endpoints {
  EVERYTHING = 'everything',
  TOP_HEADLINES = 'top-headlines',
  SOURCES = 'sources',
}
